import { Request, Response } from "express";
import asyncHandler from "express-async-handler";

export const getConversations = asyncHandler((req: Request, res: Response) => {});
export const getConversation = asyncHandler((req: Request, res: Response) => {});
